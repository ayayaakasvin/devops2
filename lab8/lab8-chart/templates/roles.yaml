apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "lab8-chart.fullname" . }}-role
rules:
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "watch", "list"]