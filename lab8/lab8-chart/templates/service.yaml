apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab8-chart.fullname" . }}
  labels:
    {{- include "lab8-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab8-chart.selectorLabels" . | nindent 4 }}
